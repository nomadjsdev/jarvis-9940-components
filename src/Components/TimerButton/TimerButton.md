**This button is designed to send a fixed message to the group - it has two states.**

#### Timer not running

```js
<TimerButton>TimerButton</TimerButton>
```

#### Timer running

```js
<TimerButton active={true}>TimerButton</TimerButton>
```
